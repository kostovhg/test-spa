<li class="list-group-item d-flex justify-content-between align-items-center">
    <span class="badge badge-primary badge-{{bootstrap-color}}">{{skillName}}</span>
    <div class="progress w-75">
        <div class="progress-bar progress-bar-striped progress-bar-animated bg-{{bootstrap-color}}" role="progressbar" aria-valuenow="{{level}}" aria-valuemin="0"
            aria-valuemax="100" style="width: {{level}}%;">{{level}}%</div>
    </div>
</li>